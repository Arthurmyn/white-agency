[build]
  publish = "public"

[functions]
  directory = "netlify/functions"

# All /api/* calls → our Express function (no :splat — preserves full path)
[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/api"
  status = 200

# /admin → static admin.html
[[redirects]]
  from   = "/admin"
  to     = "/admin.html"
  status = 200
